{% extends 'layout.html.twig' %}

{% block body %}
    <h2>OAuth Error</h2>
    <h3>{{ error }}</h3>
    <p>{{ error_description }}</p>
    <a href="{{ retry_url }}" class="btn btn-default">Retry</a>
{% endblock %}